# Documentation map

- `QA.md` — verification matrix, known limitations, and honest playtest status.
- `../README.md` — project entry point and local run commands.
- `../ROADMAP.md` — living delivery status.

The source of truth for mechanics is `src/core/model.ts`; room lessons and checked solution plans are in `src/core/rooms.ts`.
